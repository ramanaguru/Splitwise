package com.example.Splitwise.models;

public enum UserExpenseType {
    PAID,
    HAS_TO_PAY
}

/*
    The UserExpenseType enum is an enumeration that represents the type of expense that a user has.
    It has two values: PAID and HAS_TO_PAY, which represent the two possible types of expenses that a user can have.

    The enum is used in the Expense class to represent the type of expense that a user has, and it is used in the application's logic to determine
    how to calculate the user's share of the expense.


 */