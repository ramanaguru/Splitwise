package com.example.Splitwise.models;

public enum ExpenseType {

    REAL,
    DUMMY
}


/*

    The ExpenseType enum is an enumeration that represents the type of expense.
    It has two values: REAL and DUMMY, which represent the two possible types of expenses that can be created in the application.

    The enum is used in the Expense class to represent the type of expense, and it is used in the application's logic to determine how to handle the expense.

 */
